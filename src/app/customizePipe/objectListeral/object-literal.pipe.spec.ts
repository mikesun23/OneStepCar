import { ObjectLiteralPipe } from './object-literal.pipe';

describe('ObjectLiteralPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectLiteralPipe();
    expect(pipe).toBeTruthy();
  });
});
